# Vite Frontend Environment Variables - Production/Vercel Deployment
# Note: Only variables prefixed with VITE_ are exposed to the client

# API Configuration 
# CRITICAL: Must match your Render backend domain exactly
# Format: https://domain/api/ (with trailing slash)
# Override this via Vercel Project Settings > Environment Variables for custom domains
VITE_API_URL=https://pie-global-funitures.onrender.com/api/
