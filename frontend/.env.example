# Vite Frontend Environment Variables - Configuration Template
# Copy to .env for local development

# API Configuration
# Development: Point to local Django backend
VITE_API_URL=http://localhost:8000/api

# Production (Vercel): Point to deployed backend (Render, Railway, etc.)
# VITE_API_URL=https://your-backend.onrender.com/api/

# Note for Vercel deployment:
# Set VITE_API_URL in Vercel Project Settings > Environment Variables
# to avoid hardcoding production URLs in the repository

# IMPORTANT: The backend now sends absolute URLs for all media files
# You do NOT need to set VITE_S3_BASE_URL or any media URL configuration
# The frontend will trust the URLs provided by the backend API
